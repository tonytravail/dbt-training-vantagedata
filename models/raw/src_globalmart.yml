version: 2

sources:
    - name : globalmart
      database : raw
      schema: globalmart
      tables:
        - name : customers
        - name : orders
        - name : product
            columns:
                loaded_at_fields: loadedat
                freshness: 
                    warn_after: {count:12, period:hour}
                    error_after: {count:24, period: hour}